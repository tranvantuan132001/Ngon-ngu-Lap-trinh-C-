#include <stdio.h>
int main()
{
	int i,a;
		printf("a=");
		scanf("%d",&a);
		if(a%2==0){
		printf("A la so chan");
		}
		// in ra cac so chan nho hon a
		while((a%2==0)<a)
		{
		printf("cac so chan nho hon a la: ",&a);
		
		}
		return 0;
}
